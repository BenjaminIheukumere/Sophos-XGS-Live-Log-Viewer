<Window x:Class="SophosXgsLiveLogViewer.App.MasterPasswordWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Sophos XGS Live Log Viewer by Benjamin Iheukumere | Safelink IT - Profile Vault"
        Height="260"
        Width="440"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Icon="Assets/AppIcon.ico"
        Background="{StaticResource AppSurfaceBrush}"
        KeyDown="Window_KeyDown">
    <Grid Margin="18">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock x:Name="TitleText"
                   FontSize="18"
                   FontWeight="SemiBold"
                   Margin="0,0,0,14" />

        <Grid Grid.Row="1" Margin="0,0,0,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="130" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <TextBlock VerticalAlignment="Center" Text="Masterpasswort" />
            <PasswordBox x:Name="MasterPasswordBox" Grid.Column="1" MinHeight="28" />
        </Grid>

        <Grid x:Name="ConfirmRow" Grid.Row="2" Margin="0,0,0,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="130" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <TextBlock VerticalAlignment="Center" Text="Wiederholen" />
            <PasswordBox x:Name="ConfirmPasswordBox" Grid.Column="1" MinHeight="28" />
        </Grid>

        <TextBlock x:Name="ErrorText"
                   Grid.Row="3"
                   Foreground="#FF6B7A"
                   TextWrapping="Wrap" />

        <StackPanel Grid.Row="4"
                    Orientation="Horizontal"
                    HorizontalAlignment="Right">
            <Button Content="Abbrechen" Click="Cancel_Click" />
            <Button Content="OK"
                    Background="{StaticResource AccentBrush}"
                    Foreground="White"
                    IsDefault="True"
                    Click="Ok_Click" />
        </StackPanel>
    </Grid>
</Window>
